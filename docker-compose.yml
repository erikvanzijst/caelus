version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////workspace/caelus.db
    volumes:
      - .:/workspace
    # Use the same user as in Dockerfile (root by default)
    # If you later switch to a non‑root user, adjust accordingly
