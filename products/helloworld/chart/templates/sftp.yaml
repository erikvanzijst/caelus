{{- /* Read-only SFTP access to the data PVC. See the ssh-sidecar library
       chart (products/_lib/ssh-sidecar-chart) for the full contract. */}}
{{ include "ssh-sidecar.sftp.resources" (dict "root" .) }}
