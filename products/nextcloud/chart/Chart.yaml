apiVersion: v2
name: nextcloud
description: >-
  Lean Caelus-native chart for Nextcloud: the Nextcloud Deployment plus a bundled
  PostgreSQL StatefulSet on the official postgres image.
type: application
version: 0.1.12
appVersion: "34.0.2"
keywords:
  - nextcloud
  - files
  - storage
  - postgres
maintainers:
  - name: Caelus Team

# Only dependency is Freepod's own SFTP library chart.
dependencies:
  - name: ssh-sidecar
    version: "0.4.2"
    repository: "file://../../_lib/ssh-sidecar-chart"
