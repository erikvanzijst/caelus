[project]
name = "caelus-pricing"
version = "0.1.0"
description = "Caelus pricing model explorer"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.45,<2",
    "pandas>=2.0,<3",
]

[tool.uv]
dev-dependencies = []
